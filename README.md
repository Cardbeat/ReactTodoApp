# Simple React app built with Webpack and Nodejs


clone the repository:
```
 git clone git@github.com:Cardbeat/ReactTodoApp.git
 ```
 
 Install the dependencies :
 
 
 ```
 npm install
 ```
 
 
 run the app at localhost:3000
 
 ```
 npm start
 ```
 
